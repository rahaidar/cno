<div class="page-width">
  <div class="grid">
    <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
      {% if page.handle == 'join-mailing-list' %}
        {% include 'page.mailinglist' %}      
      {% else %}
        {{ page.content }}
      {% endif %}
      </div>
    </div>
  </div>
</div>


{% if page.handle == 'tobacco' or page.handle == 'charcoal' or page.handle == 'hookahs' or page.handle == 'accessories' %}
  {% include 'categories' %}   
{% endif %}